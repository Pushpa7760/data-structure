#include <stdio.h>
#define CAP 40
void printarray(int arr[], int n) {
    for(int i = 0; i < n && i<CAP ; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
int main() {
    int arr[CAP] ={10,38,789,75478,63726,77,78};
    int n = 10;
    printarray(arr, n);  
    return 0;
}
